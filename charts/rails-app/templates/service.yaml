apiVersion: v1
kind: Service
metadata:
  name: rails
  labels:
    app: rails
spec:
  selector:
    app: rails
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: {{ .Values.service.type }}

